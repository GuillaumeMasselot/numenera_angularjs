// Create the module and define its dependencies.
module App {
    angular.module("BlankApp", [
        // Angular modules 
        "ngMaterial",
        "ui.router"
        // Custom modules 

        // 3rd Party Modules
    ]);
}

